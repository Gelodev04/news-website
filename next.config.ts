import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['akns-images.eonline.com', 'media.wired.com', 'media.cnn.com','aviationa2z.com', 'other-allowed-domain.com', 'i.abcnewsfe.com',    ], // Add your image hostname here
  },
};


export default nextConfig;
